{
  "name": "finx-accelerator-microservices",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "nx run-many --target=build --all",
    "test": "nx run-many --target=test --all",
    "lint": "nx run-many --target=lint --all"
  },
  "devDependencies": {
    "@nx/gradle": "^17.0.0",
    "@nx/workspace": "^17.0.0",
    "nx": "^17.3.2"
  }
}
